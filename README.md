# Image Gallery

Internship task: a static **HTML/CSS/JavaScript** image gallery with category pages (nature, city, animals, etc.).

```bash
git clone https://github.com/hamza2324/Image_Gallery.git
cd Image_Gallery
```

Open `index.html` or `gallery.html` in a browser.

This is a front-end learning project, not a CMS.
